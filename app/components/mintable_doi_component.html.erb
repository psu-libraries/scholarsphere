<% if minting_status_doi_component.render? %>
  <%= render minting_status_doi_component %>
<% else %>
  <%= button_to t('resources.doi.create'),
                resource_doi_path(resource.uuid),
                class: 'btn btn-primary btn-sm',
                data: {
                  confirm: t('resources.doi.confirm'),
                  disable_with: t('resources.doi.disable_with')
                } %>
<% end %>
