<%= link_to path,
            method: :delete,
            class: html_class,
            data: { confirm: confirm, toggle: 'tooltip', placement: 'top', title: tooltip } do %>
            <%= button_text %><span class="subtitle responsive-hide"> <%= button_subtitle %></span>
<% end %>
